# 博客网站功能缺口分析

## 一、需求 vs 实现对比

基于 `需求分析文档.md`，以下是功能实现状态：

| 功能模块 | 需求 | 后端实现 | 前端实现 | 状态 |
|---------|------|---------|---------|------|
| **用户注册** | 邮箱注册 | ✅ | ✅ | 完成 |
| **用户登录** | 账号密码登录 | ✅ | ✅ | 完成 |
| **个人信息管理** | 修改头像、昵称、密码 | ❌ | ❌ | **缺失** |
| **文章发布** | Markdown实时预览 | ✅ | ✅ | 完成 |
| **文章编辑** | 修改已发布文章 | ✅ | ❌ | **前端缺失** |
| **文章删除** | 删除自己文章 | ✅ | 隐含 | 部分完成 |
| **文章分类** | 按分类管理 | ✅ | ✅ | 完成 |
| **文章标签** | 添加标签 | ✅(实体) | ✅ | 完成 |
| **文章列表** | 分页、排序、筛选 | ⚠️(有bug) | ⚠️(无筛选) | **需完善** |
| **文章详情** | Markdown渲染 | ✅ | ✅ | 完成 |
| **文章搜索** | 全文搜索 | ❌ | ❌ | **缺失** |
| **工具发布** | 工具信息录入 | ✅ | ✅ | 完成 |
| **工具编辑** | 修改工具信息 | ✅ | ✅ | 完成 |
| **工具删除** | 删除自己工具 | ✅ | 隐含 | 部分完成 |
| **工具分类** | 按分类管理 | ✅ | ✅ | 完成 |
| **工具标签** | 添加标签 | ✅(实体) | ❌ | **前端缺失** |
| **工具列表** | 分页、排序、筛选 | ⚠️(有bug) | ❌(无分页) | **需完善** |
| **工具详情** | 展示工具信息 | ✅ | ✅ | 完成 |
| **工具搜索** | 全文搜索 | ❌ | ❌ | **缺失** |
| **评论发布** | 对文章/工具评论 | ✅ | ✅ | 完成 |
| **评论回复** | 回复评论 | ⚠️(有父评论字段) | ❌ | **前端缺失** |
| **评论删除** | 删除自己的评论 | ❌ | ❌ | **缺失** |
| **分类管理** | 创建/编辑/删除分类 | ❌ | ❌ | **完全缺失** |
| **标签管理** | 创建/编辑/删除标签 | ❌ | ❌ | **完全缺失** |
| **按分类浏览** | 筛选分类内容 | ⚠️(有接口) | ❌ | **前端缺失** |
| **按标签浏览** | 筛选标签内容 | ⚠️(有接口) | ❌ | **前端缺失** |
| **搜索功能** | 全站搜索 | ❌ | ❌ | **完全缺失** |
| **用户中心** | 我的文章/收藏 | ❌ | ❌ | **完全缺失** |
| **点赞功能** | 点赞文章/工具 | ❌ | ⚠️(只有按钮) | **需实现** |
| **收藏功能** | 收藏文章/工具 | ❌ | ⚠️(只有按钮) | **需实现** |
| **分享功能** | 分享文章/工具 | ❌ | ⚠️(只有按钮) | **需实现** |
| **后台管理** | 内容审核、用户管理 | ❌ | ❌ | **完全缺失** |
| **数据统计** | 浏览量、评论数统计 | ⚠️(基础) | ❌ | **需完善** |

---

## 二、核心缺失功能（按优先级）

### 🔴 P0 - 必须立即补充

#### 1. 文章编辑功能（前端缺失）
**现状**: `WriteArticleView.vue` 只能新建文章，没有编辑功能
**需求**: 用户能够编辑自己发布的文章
**实现方案**:
- 后端已有 `PUT /articles/{id}` 接口
- 前端需要新增 `EditArticleView.vue` 页面
- 路由: `/article/:id/edit`

#### 2. 搜索功能（完全缺失）
**现状**: 没有任何搜索接口和UI
**需求**: 支持按标题、内容、标签搜索文章和工具
**实现方案**:
- 后端新增搜索Controller
- 使用MySQL全文索引或LIKE查询（短期）
- Elasticsearch（长期）
- 前端添加搜索框组件

#### 3. 用户中心/个人中心（完全缺失）
**现状**: 无任何个人中心页面
**需求**: 用户查看自己的文章、收藏、评论等
**实现方案**:
- 后端新增 `/user/me` 接口获取当前用户信息
- 后端新增 `/user/articles` 获取用户文章列表
- 后端新增 `/user/tools` 获取用户工具列表
- 前端新增 `ProfileView.vue` 页面
- 路由: `/profile`

#### 4. 分类/标签管理（完全缺失）
**现状**: 分类和标签只有静态数据，无法增删改
**需求**: 管理员能够管理分类和标签
**实现方案**:
- 后端新增 `CategoryController` 和 `TagController`
- 前端新增管理页面（或内置到后台）

### 🟡 P1 - 短期补充

#### 5. 评论回复功能（前端缺失）
**现状**: 数据库有 `parent_id` 字段，后端有接口，但前端没有回复UI
**需求**: 用户能够回复评论
**实现方案**:
- 前端修改 `ArticleDetailView.vue` 和 `ToolDetailView.vue`
- 添加回复输入框和回复列表展示

#### 6. 标签筛选功能（前端缺失）
**现状**: 文章有标签数据，但无法按标签筛选
**需求**: 点击标签可筛选相关内容
**实现方案**:
- 后端新增 `/articles?tag_id=xxx` 和 `/tools?tag_id=xxx` 接口
- 前端在列表页添加标签筛选UI

#### 7. 分类筛选功能（前端缺失）
**现状**: 文章有分类数据，但无法按分类筛选
**需求**: 点击分类可筛选相关内容
**实现方案**:
- 后端已有 `/articles?category_id=xxx` 接口（需确认）
- 前端在列表页添加分类筛选UI

#### 8. 点赞功能（需实现）
**现状**: 只有按钮，无实际逻辑
**需求**: 用户可点赞文章/工具
**实现方案**:
- 数据库新增点赞表（或Redis缓存）
- 后端新增点赞接口
- 前端绑定点击事件

### 🟢 P2 - 中期补充

#### 9. 收藏功能（需实现）
**现状**: 只有按钮，无实际逻辑
**需求**: 用户可收藏文章/工具
**实现方案**:
- 数据库新增收藏表
- 后端新增收藏接口
- 前端绑定点击事件

#### 10. 分享功能（需实现）
**现状**: 只有按钮，无实际逻辑
**需求**: 分享文章/工具链接
**实现方案**:
- 使用浏览器原生 `navigator.share` API
- 或复制链接到剪贴板

#### 11. 草稿保存（后端缺失）
**现状**: 前端有"保存草稿"按钮，但后端无接口
**需求**: 用户可保存草稿
**实现方案**:
- 后端新增草稿相关接口
- 或复用现有文章接口，添加"草稿"状态

#### 12. 评论删除（后端+前端缺失）
**现状**: 无删除功能
**需求**: 用户可删除自己的评论
**实现方案**:
- 后端新增 `DELETE /comments/{id}` 接口
- 前端添加删除按钮

---

## 三、详细实现建议

### 3.1 搜索功能实现方案

**后端（新增 SearchController）**:
```java
@RestController
@RequestMapping("/search")
public class SearchController {
    
    @GetMapping("/articles")
    public ResponseEntity<?> searchArticles(@RequestParam String keyword) {
        // 使用MySQL全文索引或LIKE查询
        List<Article> articles = articleRepository.searchByKeyword(keyword);
        return ResponseEntity.ok(articles);
    }
    
    @GetMapping("/tools")
    public ResponseEntity<?> searchTools(@RequestParam String keyword) {
        List<Tool> tools = toolRepository.searchByKeyword(keyword);
        return ResponseEntity.ok(tools);
    }
}
```

**前端（新增 SearchBar 组件）**:
```vue
<template>
  <div class="search-bar">
    <el-input v-model="keyword" placeholder="搜索文章或工具..." clearable>
      <template #prefix>
        <Icon name="search" size="sm" />
      </template>
    </el-input>
    <el-button type="primary" @click="handleSearch">搜索</el-button>
  </div>
</template>
```

### 3.2 用户中心实现方案

**后端（新增 UserController 接口）**:
```java
@GetMapping("/me")
public ResponseEntity<?> getCurrentUser() {
    Authentication auth = SecurityContextHolder.getContext().getAuthentication();
    User user = userRepository.findByUsername(auth.getName());
    return ResponseEntity.ok(user);
}

@GetMapping("/me/articles")
public ResponseEntity<?> getMyArticles(@RequestParam int page, @RequestParam int size) {
    Authentication auth = SecurityContextHolder.getContext().getAuthentication();
    User user = userRepository.findByUsername(auth.getName());
    // 分页查询用户文章
    Page<Article> articles = articleRepository.findByUserId(user.getId(), PageRequest.of(page, size));
    return ResponseEntity.ok(articles);
}

@GetMapping("/me/tools")
public ResponseEntity<?> getMyTools(@RequestParam int page, @RequestParam int size) {
    // 类似文章逻辑
}
```

**前端（新增 ProfileView.vue）**:
- 我的文章列表
- 我的工具列表
- 我的收藏列表（待实现）
- 个人资料编辑

### 3.3 分类/标签管理实现方案

**后端（新增 CategoryController 和 TagController）**:
```java
@RestController
@RequestMapping("/categories")
public class CategoryController {
    // 管理员接口：增删改查
}

@RestController
@RequestMapping("/tags")
public class TagController {
    // 管理员接口：增删改查
}
```

### 3.4 评论回复实现方案

**前端修改 ArticleDetailView.vue**:
```vue
<!-- 评论卡片 -->
<div class="comment-card" v-for="comment in comments" :key="comment.id">
  <!-- 主评论 -->
  <div class="comment-main">
    <p>{{ comment.content }}</p>
  </div>
  <!-- 回复按钮 -->
  <el-button @click="showReply(comment)">回复</el-button>
  
  <!-- 回复列表 -->
  <div v-if="comment.replies && comment.replies.length > 0" class="reply-list">
    <div v-for="reply in comment.replies" :key="reply.id" class="reply-item">
      <span>{{ reply.username }}:</span>
      <span>{{ reply.content }}</span>
    </div>
  </div>
  
  <!-- 回复输入框（点击回复后显示） -->
  <el-input v-if="replyingComment?.id === comment.id" 
            v-model="replyContent" 
            placeholder="输入回复..." />
</div>
```

---

## 四、数据库变更建议

### 4.1 新增点赞表
```sql
CREATE TABLE like_record (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    target_id BIGINT NOT NULL,
    target_type VARCHAR(20) NOT NULL,  -- 'article' or 'tool'
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES user(id)
);
```

### 4.2 新增收藏表
```sql
CREATE TABLE favorite (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    target_id BIGINT NOT NULL,
    target_type VARCHAR(20) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES user(id)
);
```

### 4.3 文章表新增草稿状态
```sql
ALTER TABLE article ADD COLUMN status VARCHAR(20) DEFAULT 'published';
-- status: draft(草稿), published(已发布)
```

---

## 五、前端页面规划

### 5.1 新增页面
| 页面 | 路由 | 说明 |
|-----|------|------|
| EditArticleView | `/article/:id/edit` | 编辑文章 |
| ProfileView | `/profile` | 个人中心 |
| AdminView | `/admin` | 后台管理（可选） |
| SearchView | `/search` | 搜索结果页（可选） |

### 5.2 修改现有页面
| 页面 | 修改内容 |
|-----|---------|
| ArticleListView | 添加搜索框、分类筛选、标签筛选 |
| ToolListView | 添加搜索框、分类筛选、标签筛选 |
| ArticleDetailView | 完善评论回复功能 |
| ToolDetailView | 完善评论回复功能 |
| App.vue | 添加用户中心入口 |

---

## 六、API接口补充清单

### 6.1 搜索相关
- `GET /search/articles?keyword=xxx`
- `GET /search/tools?keyword=xxx`

### 6.2 用户中心
- `GET /auth/me` - 获取当前用户信息
- `GET /auth/me/articles?page=0&size=10` - 我的文章
- `GET /auth/me/tools?page=0&size=10` - 我的工具
- `PUT /auth/profile` - 更新个人信息

### 6.3 分类/标签管理（管理员）
- `POST /categories` - 创建分类
- `PUT /categories/{id}` - 更新分类
- `DELETE /categories/{id}` - 删除分类
- `POST /tags` - 创建标签
- `PUT /tags/{id}` - 更新标签
- `DELETE /tags/{id}` - 删除标签

### 6.4 互动功能
- `POST /articles/{id}/like` - 点赞文章
- `DELETE /articles/{id}/like` - 取消点赞
- `POST /tools/{id}/like` - 点赞工具
- `POST /articles/{id}/favorite` - 收藏文章
- `DELETE /articles/{id}/favorite` - 取消收藏
- `DELETE /comments/{id}` - 删除评论

---

## 七、实施顺序建议

### 第一阶段（1-2天）
1. 实现文章编辑功能（前端EditArticleView + 路由）
2. 实现用户中心基础功能（获取用户信息、我的文章列表）
3. 添加搜索框到首页和列表页（先做搜索框UI）

### 第二阶段（2-3天）
1. 实现完整搜索功能（后端SearchController + 前端搜索逻辑）
2. 实现分类/标签筛选功能
3. 实现评论回复功能

### 第三阶段（2-3天）
1. 实现点赞功能
2. 实现收藏功能
3. 实现分享功能
4. 实现评论删除功能

### 第四阶段（可选）
1. 实现后台管理面板
2. 实现数据统计看板
3. 实现草稿保存功能
4. 完善用户资料编辑功能
